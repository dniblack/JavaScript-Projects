function not_Function() {
    document.getElementById("Me").innerHTML = !(20 > 10); // In the booelean comparison, I'm using the logical operator ! to compare both sides of the comparison; the return should be fALSE
    }
    
function not2_Function() {
    document.getElementById("Me_2").innerHTML = !(5 > 10); // In the booelean comparison, I'm using the logical operator ! to compare both sides of the comparison; the return should be true
    }

    document.write(2 * 4 * 8 * 16 == 2 * 4 * 8 * 16) // using the double equal signs to show that both sides of the comparison are equal to each other
    document.write("<br>");                         // line break for organization
    x = "Jurassic Park released in the year of 199X"; // giving the object "x" a value
    y = "Jurassic Park released in the year of 199x"; // giving the object "y" a value
    console.write = (x === y); // Used the triple equal sign to compare data types in values; will return false since 199-'s are different
    document.write("<br>"); // line break for organization
    document.write(5 > 4 && 2 < 67); // In the boolean comparison, I'm using the logical operator && to compare both sides of the comparison; the return should be true
    document.write("<br>"); // line break for organization
    document.write(115 > 34 || 12 < 5); // In the boolean comparison, I'm using the logical operator || to compare both sides of the comparison; the return should be true
    document.write("<br>"); // line break for organization
    document.write(typeof "Breath of the Wild"); // Used the typeof operator to return the data type of thevariable; the output should be "string"
    document.write("<br>"); // line break for organization
    document.write(25 > 14); // In the boolean comparison, I'm using the less than sign to show 25 is less than 14
    document.write("<br>"); // line break for organization
    document.write(72 < 90); // In the boolean comparison, I'm using the less than sign to show 72 is less than 90